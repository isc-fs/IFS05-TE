import QtQuick

Rectangle {
    id: components
    anchors {
        top: parent.top
        bottom: bottombar.top
        right: rightscreen.left
        left: parent.left
    }

    color: "white"
}
